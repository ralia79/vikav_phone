import 'package:flutter/material.dart';

class ShortCutScreen extends StatelessWidget {
  const ShortCutScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("ShortCutScreen"),
      ),
    );
  }
}